# Bring-your-own AI interview

Add optional AI-guided discussion alongside existing wizard. No backend, accounts, analytics, or publisher keys. Users explicitly choose destination/model and consent to sending the CURRENT question, current saved answer and chat for that question to chosen endpoint. No whole-session/other-answer sharing. Keys and chat memory-only, no storage/export/logging. Existing normal mode sends only static GETs. Preserve gold/ivory design and existing exports. This is a setup plan, no installation actions.

Transport owner generator_repair: v2/ai-client.mjs and tests/ai-client.mjs only. UI owner v2_ui: v2/app.mjs v2/index.html v2/styles.css and tests/ai-browser.mjs only. Parent: build/server allowlists, README/privacy, docs rebuild/publication. No worker commits/push. Not alone: preserve others' work.

Contract ai-client.mjs exports validateConnection({endpoint,apiKey,model}) => normalized same object; requestGuidance({endpoint,apiKey,model,question,currentAnswer,messages,signal}) => Promise<{reply:string,proposal:null|string|string[]}>. question is catalog object with id/title/type/help/options. messages array only user/assistant string content. OpenAI-compatible POST chat completions endpoint exact userentered URL. HTTPS only except http localhost/127.0.0.1/[::1]; reject credentials/query/hash, malformedURL, no /chat/completions suffix, model missing or over200chars, key missing exceptloopback (max4096). Do not auto-discover models or fallback destinations. fetch credentials omit, redirect error, cache no-store, referrerPolicy no-referrer. Timeout45s and caller cancellation. Bound messages20*4000chars/currentAnswer12000 and responsebody128KiB (stream bounded). Generic errorstatus UX401/403/429/5xx/CORS, never echo providererrorbodies/key. Model request ONLY systeminstruction/currentquestion/currentanswer/messagehistory no key body. No tools. max_tokens1000 streamfalse. Request JSON reply+proposal without mandatory response_format for modelcompatibility. System says explain/askfollowups, do not inventpreferences, propose currentquestion answer only, choice values exact, no executions. Parse fenced/plainJSON or fallback plainreply withoutproposal; strict reply<=12000, proposal matching questiontype/options (undecided mutualexclusion validated by engine atUI). Renderedreply plaintext. Never return APIkey inreply/proposal; reject usermessages containing actualkey before request. Tests cover endpoints, body privacy, statuses, abort/timeout, huge response, malformedJSON, malicious outputtypes, no redirects/credentials.

UI: add Guide me with AI entry on landing (start/resume selected mode then openAI) and question page. AI connection dialog: OpenRouter preset endpoint https://openrouter.ai/api/v1/chat/completions OR custom compatible endpoint including local. No selected modeldefault; explicitmodel textbox; passwordkey autocompleteoff; no persistence. Clear label this is interviewer model not automaticallyHermes model. Display exact destination and consent checkbox unchecked; never request until user Connect then sends chat. Connection can be changed/disconnected; clearing discards key/input/history. Page refresh clears key. Modal chat shows current question static; user sendsmessage; assistant can discuss and propose answer card with explicit Use this answer button. Button calls answerQuestion(), persist(), advance() keeping chat open for nextquestion and clearing previoushistory. Never autosave suggestion. Allowcontinuedclarification, dismisscard. Back/skip/import/reset/modechanges invalidate/abort pendingrequests and clear stale proposals. ClosingAI aborts request; no late reply maymutate currentquestion/session. UI controls race-safe senddisabledbusy, cancel button, errors visible with retry byuser only, no autoretry. Ensure Enter multiline sensible form, focus, narrowmobile. Untrusted text escaped/textContent only. Never put credentials in logs/storage/interview/downloads/URL. Disclose provider sees chats, may charge; plainwizard stayslocal. Header/footer/agentdialog privacy copy updated to distinguish optional directAI.

Testing: node tests/ai-client.mjs; new realbrowser mockednetwork tests/ai-browser.mjs for explicitconsent/no preconnectrequests, actualquestioncontext, discussion/proposalaccept->nextquestion, invalidproposals no save, XSSliteral, connectiondisconnect removes key, session/export excludeskey/chat, reload forgetsconnection, race abort/switchquestion, HTTPerror. Reuse existing Playwright tooling. No realkeys/calls charged. Full previous engine/browser/sitebuild checks preserved. Document only OpenAI-compatible chat APIs with browser CORS support; native Anthropic API unsupported. CORS blocked/local restrictions explained without asking usersdisable browsersecurity.
